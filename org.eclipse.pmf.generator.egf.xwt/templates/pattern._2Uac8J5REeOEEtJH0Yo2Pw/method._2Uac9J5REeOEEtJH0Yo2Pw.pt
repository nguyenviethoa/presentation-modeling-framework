<%	if (dataItem.getLabel() != null) {
		if (parameter == dataItem.getLabel()) {
			label = parameter;
		}
	}	
	if (dataItem.getHelp() != null) {
		if (parameter == dataItem.getHelp()) {
			help = parameter;
		}
	}
	
	String[] nsPair = XWTHelper.addNamespace(ctx, "org.eclipse.epf.richtext.extension.RichTextEditor");
	nameString = nsPair[1];
%>
 <<%=nsPair[1]%>:RichTextEditor 
		